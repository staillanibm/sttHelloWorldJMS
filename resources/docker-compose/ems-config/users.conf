#
# users.conf - TIBCO EMS user declarations
#
# Format: <user-name>:[password]:<description>
# WARNING: passwords are managed by EMS only - never set them manually here.
#

# Built-in administrator (empty password by default)
admin::Administrator

# Server identity for routing (must match server name in tibemsd.conf)
EMS-SERVER::EMS Server

# Example application user:
# appuser::Application Service Account
